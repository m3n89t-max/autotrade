NEoWave 기반 지식학습 시스템 (RAG + Rule DB 구조)

버전: v1.0
작성일: 2026-02-21
범위: 모델 재학습 없이, 사용자 자료를 규칙/지식 DB로 구조화하여 엔진에서 참조하는 구조

1. 설계 목적

본 모듈의 목적은:

사용자 제공 자료(텍스트, PDF, PNG, 자막 등)를
NEoWave 규칙/용어/예외/전략 조건으로 구조화하여
Wave Engine / Strategy Engine / AI Chat에서 근거 기반으로 참조하도록 하는 것.

⚠️ 본 구조는 모델 파인튜닝이 아닌, RAG + Rule DB 기반 지식학습 구조이다.

2. 전체 구조 개요
[User Upload]
   ├─ Text / Markdown
   ├─ PDF
   ├─ PNG (OCR)
   ├─ Video Subtitle (SRT/VTT)
   └─ User Summary

        ↓

[Text Extraction / OCR]

        ↓

[Chunking]

        ↓

[RAG Index (Vector DB)]
        +
[Rule Candidate Extraction (If/Then DSL)]

        ↓

[Rule Review UI]
 (승인 / 수정 / 폐기)

        ↓

[Rule DB (Active Rules)]
        ↓
Wave Engine / Strategy Engine / AI Chat
3. 입력 자료 범위
3.1 지원 형식
형식	처리 방식
TXT/MD	직접 파싱
PDF	텍스트 추출
PNG/JPG	OCR 후 텍스트 변환
SRT/VTT	자막 파싱
사용자 요약	직접 입력

⚠️ 동영상 파일(mp4 등)은 직접 처리하지 않음.
권장 방식: 자막 파일 업로드 또는 사용자 요약 입력

4. 텍스트 처리 파이프라인
4.1 텍스트 추출
PNG/JPG

OCR 엔진 사용 (예: Tesseract)

해상도 기준: 최소 300dpi 권장

결과 텍스트를 raw_text로 저장

4.2 Chunking

500~1000 토큰 단위 분할

각 chunk에:

source_id

chunk_index

page_number(가능한 경우)

start/end offset 저장

5. RAG (Retrieval Augmented Generation) 구조
5.1 Vector Index 생성

각 chunk를 벡터화하여 저장:

{
  "chunk_id": "...",
  "embedding": [...],
  "source_id": "...",
  "text": "..."
}
5.2 AI 응답 시 동작

질문/분석 컨텍스트 입력

관련 chunk Top-K 검색

해당 텍스트를 근거로 AI 답변 생성

“근거 표시 카드” 함께 출력

6. Rule DSL 구조 정의
6.1 목적

사용자 자료에서 실제 엔진에서 실행 가능한 규칙(If/Then) 으로 변환

6.2 Rule 데이터 구조
rule_id: UUID
rule_type: wave.validation | wave.invalidation | strategy.entry | strategy.exit | risk | econ.blackout
name: "Wave4 overlap 금지"
priority: 90
if:
  - condition: "overlap(wave1.high, wave4.low) == true"
then:
  - verdict: "invalid"
exceptions:
  - condition: "pattern == diagonal"
    then: { verdict: "allowed" }
applies_to:
  timeframe: ["4H","1H","15M"]
  market: ["crypto"]
source:
  doc_id: "..."
  chunk_id: "..."
confidence: 0.83
status: draft | active | deprecated
7. Rule Candidate Extraction
7.1 자동 추출 단계

chunk 텍스트에서:

“must”, “should”, “invalid”, “cannot”, “only if” 등 조건 문장 탐지

NEoWave 용어 기반 패턴 매칭:

overlap

retracement %

time ratio

extension

terminal

complex correction

→ Rule 후보 생성

7.2 검수 UI 필수

자동 생성된 규칙은:

좌측: 원문 텍스트

우측: Rule DSL 변환 결과

사용자가:

✅ 승인

✏️ 수정

⛔ 폐기

🔼 우선순위 조정

승인 시 status=active

8. Rule Engine 연동
8.1 Wave Engine 적용

rule_type=wave.validation/invalidation 만 적용

invalid 시 대안 시나리오로 전환

8.2 Strategy Engine 적용

rule_type=strategy.entry/exit 사용

조건 충족 시 자동매매 트리거 후보 생성

8.3 Risk Engine 적용

rule_type=risk

연속 손실 제한

경제지표 차단

포지션 제한

9. Glossary (용어 사전)

NEoWave 특수 용어 교정/정규화 목적

용어	설명	동의어
overlap	1파와 4파 겹침	겹침
retracement	되돌림	되돌림비율
terminal	종단형	diagonal

OCR 오인식 보정에 활용

10. AI Chat에서의 동작

AI는 답변 시:

활성 규칙 검색

관련 chunk RAG 검색

규칙 근거 표시

출력 예:

Primary Scenario:
...
Invalidation:
...
근거 규칙:
- Wave4 overlap 금지 (Doc: p.124)
- Retracement < 61.8% 조건
11. 버전 관리

SourceDocument는 version 필드 보유

Rule은 source_version 참조

문서 업데이트 시:

기존 규칙은 deprecated 가능

새 규칙은 draft 상태로 생성

12. 보안/저작권 고려

원문 전체를 외부에 공개하지 않음

AI 응답 시 짧은 스니펫만 표시

출처 메타데이터만 유지

13. MVP 범위
필수

 텍스트/PDF 업로드

 PNG OCR 지원

 Chunking

 Vector DB 저장

 Rule 후보 자동 생성

 Rule Review UI

 Active Rule → Wave/Strategy Engine 연동

 AI Chat RAG 기반 근거 출력

제외(MVP 범위 아님)

모델 파인튜닝

대규모 자동 영상 전사

완전 자동 규칙 승인

14. 확장 가능성

규칙 충돌 자동 탐지

규칙별 성과 통계

전략별 Rule Weight 자동 조정

다중 사용자 지식 공유(옵션)

15. 최종 정의

본 시스템의 “AI 학습”은:

사용자 자료를 모델이 재학습하는 구조가 아니라,
규칙과 지식으로 구조화하여 엔진에서 실행 가능하도록 만드는 구조

이며,
이는 RAG + Rule DB + 통계 업데이트로 구현된다.

16. 과거 데이터 기반 테스트 (백테스트)

16.1 목적

실시간 배포 전에 Wave Engine / Strategy Engine / Rule 적용 결과를 검증하기 위해,
과거 OHLCV 데이터로 동일 로직을 재생(재실행)하여 정확도·성과·예외를 평가한다.

16.2 데이터 요구사항

| 항목 | 설명 |
|------|------|
| 형식 | OHLCV (Open, High, Low, Close, Volume) |
| 타임프레임 | 실거래와 동일 범위 지원 (1m ~ 1W) |
| 기간 | 테스트 구간 지정 가능 (시작일–종료일) |
| 출처 | CSV/DB/외부 API 등 재현 가능한 저장소 |
| 품질 | 결측·이상치 처리 규칙 정의 (보간/스킵/알림) |

16.3 테스트 범위

- **Wave Engine**: 과거 구간에 대해 파동 카운트(Primary/Alternative) 재계산, 무효화선·타겟 출력이 과거 시점과 일치하는지 검증
- **Strategy Engine**: 진입/청산 규칙을 과거 데이터에 적용한 시뮬레이션 주문 수, 체결 가정, 손익 시뮬레이션
- **Rule Engine**: 활성 Rule이 과거 구간에서 얼마나 위반/적용되었는지 통계 (invalid 비율, 예외 케이스)
- **경제지표**: 해당 구간의 High Impact 이벤트를 blackout으로 반영한 뒤 전략 수익률 비교 (이벤트 전후 차단 효과)

16.4 출력·지표

- 구간별 수익률, 최대 낙폭(MDD), 승률, 거래 횟수
- Wave 시나리오 전환 횟수, 무효화 발생 횟수
- Rule 위반/적용 건수 및 구간별 요약
- (선택) 실거래와 백테스트 결과 비교 리포트

16.5 재생(Replay) 모드

- 동일 과거 데이터를 “시간 순”으로 한 봉씩 공급하여 엔진 실행
- 실시간과 동일한 이벤트 흐름(심볼 변경, TF 변경, Rule 적용) 적용
- 재생 속도 조절(1x, N배속) 옵션 권장 (UI 연동)

16.6 MVP 범위

- 과거 OHLCV 입력 API 또는 파일 업로드
- 단일 심볼·단일 타임프레임 백테스트 실행
- 결과 요약(수익률, 거래 수, Rule 통계) API 응답
- (선택) 백테스트 결과 저장 및 비교